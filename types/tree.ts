// The shared data contract for the whole app — TreeEntry (leaf | milestone | origin) and TestimonyTree.
// Fill in the section-5 contract, and add an ownerId on the tree so a social layer (follows / Ripple) can attach later.
