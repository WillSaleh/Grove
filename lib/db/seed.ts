// Reset-to-seed script: clears all entries back to a bare seed (keeps the one user) so every demo run starts empty.
// Run this before each rehearsal and the demo. Nothing pre-loads — you build the tree live.
