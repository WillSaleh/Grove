// Table definitions, mirroring types/tree.ts: users, the tree, and its entries — include an ownerId on the tree so follows / Ripple are additive later.
// No auth yet; seed a single user. Coordinate this schema with Jackson & Josh.
