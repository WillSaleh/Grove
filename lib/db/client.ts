// Database connection — creates and exports one shared client instance. The only file that opens the DB; everything else goes through the repository.
// SQLite for the demo; swap to Postgres/Supabase behind this file when the social layer lands.
