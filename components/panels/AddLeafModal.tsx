// Modal for adding a daily leaf (verse / prayer / gratitude / reflection); on submit calls store.addLeaf so it appears in the canopy.
// Primary on-stage surface — keep required fields minimal (date defaults to today) and submit optimistically.
