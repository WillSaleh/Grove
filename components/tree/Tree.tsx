// The illustrated tree: renders store entries into canopy (leaves), trunk (milestones), and base (origin), sized by growth stage (seed → tree).
// Owns tap-to-open-detail and the answered-prayer gold bloom. This is the centerpiece — match the POC.
