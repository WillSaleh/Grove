// NOT imported at runtime — the tree is built live during the demo, so nothing pre-loads.
// Optional private draft of your entries, so on stage you're reading-and-typing instead of recalling. Safe to delete.
